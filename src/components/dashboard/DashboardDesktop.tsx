'use client';

// This component is now deprecated.
// The dashboard is powered by route-driven pages under /app/dashboard/.
// DashboardShell (sidebar + topbar) is the new layout.
export default function DashboardDesktop() {
  return null;
}
